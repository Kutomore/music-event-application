<ul class="nav navbar-nav navbar-right">
  <li>
    <p class="navbar-text vertical-center">Bem vindo <b><%= link_to current_user.profile.name, profile_path(current_user.profile) %></b></p>
    <div class="bottom text-center">
      <b><%= link_to 'Log out', destroy_user_session_path, method: :delete  %></b>
    </div>
  </li>
</ul>
