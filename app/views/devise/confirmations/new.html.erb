<h1><%= t('.resend_confirmation_instructions') %></h1>

<%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>
  <%= bootstrap_devise_error_messages! %>

  <div class="form-group">
    <%= f.label :username %>
    <%= f.text_field :username, autofocus: true, autocomplete: 'username', value: (resource.pending_reconfirmation? ? resource.unconfirmed_username : resource.username), class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.submit t('.resend_confirmation_instructions'), class: 'btn btn-primary' %>
  </div>
<% end %>

<%= render 'devise/shared/links' %>
